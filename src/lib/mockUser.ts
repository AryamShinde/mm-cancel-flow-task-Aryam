// Centralized mock user used across pages during development.
// Aligns with seeded user3@example.com in supabase/seed.sql
export const mockUser = {
  // Only keep email for mock purposes; ID intentionally omitted.
  email: 'user3@example.com'
};
